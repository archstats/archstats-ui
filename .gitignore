# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example
/terraform/.terraform/

# local dev workspace (release builds use published archstats module)
go.work
go.work.sum
build/bin

frontend/node_modules
frontend/dist/*
!frontend/dist/.gitkeep
frontend/auto-imports.d.ts
frontend/components.d.ts

# Impeccable review captures are scratch, regenerated each round. Anchored
# nowhere: the frontend keeps its own, and the root rule did not reach it.
**/.impeccable/review/
